# Debugger Configuration
# The debugger is now configured via APP_CONFIG.debugger.enabled in src/config/app-config.ts
# Set to 'true' to enable the debugger UI in the bottom right corner
# The debugger will show server logs in a terminal-like interface
